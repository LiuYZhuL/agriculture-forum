<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agriculture.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.agriculture.model.po.User">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />

        <result column="email" property="email" jdbcType="VARCHAR" />
        <result column="avatar" property="avatar" jdbcType="VARCHAR" />
        <result column="role_id" property="roleId" jdbcType="INTEGER" />

        <result column="status" property="status" jdbcType="INTEGER" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <select id="getUserById" resultType="com.agriculture.model.po.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="getAllUsers" resultType="com.agriculture.model.po.User">
        SELECT * FROM user
    </select>
    <insert id="insertUser" parameterType="com.agriculture.model.po.User">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            username, password, email
            <if test="avatar!= null" > ,avatar </if>
            <if test="roleId!= null" > ,role_id </if>
        </trim>
            VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            #{username}, #{password}, #{email}
            <if test="avatar!= null"> ,#{avatar} </if>
            <if test="roleId!= null"> ,#{roleId} </if>
        </trim>
    </insert>
    <update id="updateUser" parameterType="com.agriculture.model.po.User">
        UPDATE user
        <set>
            <if test="username!= null"> username = #{username}, </if>
            <if test="password!= null"> password = #{password}, </if>
            <if test="email != null"> email = #{email}, </if>
            <if test="avatar != null"> avatar = #{avatar}, </if>
            <if test="roleId != null"> role_id = #{roleId}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="lastLoginTime != null"> last_login_time = #{lastLoginTime}, </if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteUser">
        DELETE FROM user WHERE id = #{id}
    </delete>
    <select id="getUserByStatus" resultType="com.agriculture.model.po.User">
        SELECT * FROM user WHERE status = #{status}
    </select>

    <select id="getUserCount" resultType="int">
        SELECT COUNT(*) FROM user
    </select>
</mapper>